<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
    />
    <title>Weather Report</title>
    <style>
      body {
        background-color: #191919;
        color: #ecdbba;
        font-family: Arial, sans-serif;
        font-size: 24px;
        margin: 0;
        padding: 20px;
        box-sizing: border-box;
      }

      .section {
        text-align: center;
        background-color: #2d4263;
        padding: 10px;
        margin: 60px;
        border-radius: 10px;
      }

      h2 {
        color: #ecdbba;
        font-size: 48px;
        margin-bottom: 0px;
      }

      hr {
        color: #ecdbba;
      }

      .hourly-weather {
        display: flex;
        justify-content: space-between;
        margin-top: 10px;
        overflow: scroll;
        overflow-y: hidden;
      }

      .forecast-container {
        flex: 1;
        text-align: center;
        width: fit-content;
        padding: 0px 10px;
      }

      .forecast-data {
        width: 140px;
        padding: 10px;
        font-weight: 700;
      }
    </style>
  </head>

  <body>
    <div class="section">
      <h2><%= formattedName %></h2>
      <hr />
      <p>Latitude: <%= lat %> | Longitude: <%= lng %></p>
    </div>

    <div class="section">
      <p>Temperatures: <%= temperature %>°C</p>
      <p>Wind Speed: <%= windspeed %> km/h</p>
      <p>Wind Direction: <%= winddirection %>°</p>
    </div>

    <div class="section">
      <div class="hourly-weather">
        <% for(let i = 0; i < formattedTime.length; i++) { %>
        <div class="forecast-container">
          <div class="forecast-data">
            <p class="<%= weatherIcons[weathercode[i]] %>"></p>
            <p><%= formattedTime[i] %></p>
            <p><%= temperature_2m[i] %>°C</p>
          </div>
        </div>
        <% } %>
      </div>
    </div>
  </body>
</html>
